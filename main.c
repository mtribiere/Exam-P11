#include "fonctions.h"
#include <stdio.h>
#include <stdlib.h>

int main(){

  /*
  //Test Question 3
  Personne hive = {0,"Hive","Roger"};
  afficherNom(&hive);
  */

  /*
  //Test Question 4
  liste chaine = malloc(sizeof(struct cellule));
  chaine->valeur = 4;
  chaine->suiv = NULL;
  printf("%d\n",listeAEntier(chaine,7));
  free(chaine);
  */

  /*
  //Test Question 5
  liste chaine1 = malloc(sizeof(struct cellule));
  chaine1->valeur = 4;
  chaine1->suiv = NULL;

  liste chaine2 = malloc(sizeof(struct cellule));
  chaine2->valeur = 4;
  chaine2->suiv = NULL;

  printf("%d\n",listeEgales(chaine1,chaine2));

  free(chaine1);
  free(chaine2);
  */

  //Test Question 6
  printf("%d\n",nombreNoeud(3));
}
